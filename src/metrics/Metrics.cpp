#include "metrics/Metrics.h"

namespace gd {

Metrics MetricsCalculator::compute(const Embedding& emb, const Space& space) const {
        Metrics metrics{};
        return metrics;
}

} // namespace gd

